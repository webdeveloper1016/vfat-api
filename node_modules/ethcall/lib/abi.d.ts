import { JsonFragmentType } from '@ethersproject/abi';
export default class Abi {
    static encode(name: string, jsonInputs: JsonFragmentType[], params: any[]): string;
    static encodeConstructor(jsonInputs: JsonFragmentType[], params: any[]): string;
    static decode(name: string, jsonOutputs: JsonFragmentType[], data: string): import("@ethersproject/abi").Result;
}
