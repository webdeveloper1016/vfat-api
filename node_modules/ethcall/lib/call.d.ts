import { JsonFragmentType } from '@ethersproject/abi';
import { BaseProvider } from '@ethersproject/providers';
import { Multicall } from './multicall';
export interface Call {
    contract: {
        address: string;
    };
    name: string;
    inputs: JsonFragmentType[];
    outputs: JsonFragmentType[];
    params: any[];
}
export interface CallResult {
    success: boolean;
    returnData: string;
}
export declare function all<T>(provider: BaseProvider, multicall: Multicall | null, calls: Call[], block?: number): Promise<T[]>;
export declare function tryAll<T>(provider: BaseProvider, multicall2: Multicall | null, calls: Call[], block?: number): Promise<(T | null)[]>;
